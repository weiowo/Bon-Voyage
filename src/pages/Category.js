// 在首頁按下種類的時候，會把種類跟使用者所在位置的經緯度紀錄
// 接著到category頁面執行相關搜尋
// 不同的種類上面的banner可以不一樣

import React from 'react';

function Category() {
  console.log('我在cardCategoryPage');
  return (
    <div>種類頁面</div>
  );
}

export default Category;
